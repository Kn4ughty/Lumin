[package]
name = "lumin"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.32.0"
egui_extras = "0.32.0"
# For image support:
# egui_extras = { features = ["default", "image"] }

env_logger = { version = "0.10", default-features = false, features = [
    "auto-color",
    "humantime",
] }
